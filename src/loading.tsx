export const Loading = () => {
  return (
    <div className={"mt-24 flex items-center justify-center px-3 py-32"}>
      <div className="h-12 w-12 animate-spin rounded-full border-8 border-transparent border-r-primary border-t-primary"></div>
    </div>
  );
};

export default Loading;
